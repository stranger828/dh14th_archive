import OutputGrid from '../components/OutputGrid';

export default function ArchivePage() {
    return (
        <main className="min-h-screen pt-16 sm:pt-20 bg-white dark:bg-black">
            <OutputGrid />
        </main>
    );
}
